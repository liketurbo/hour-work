overwrite: true
schema: 'http://localhost:4000/graphql'
documents: 'graphql/**/*.ts'
generates:
  components/ApolloComponents.tsx:
    config:
      noNamespaces: true
    plugins:
      - 'typescript-common'
      - 'typescript-client'
      - 'typescript-react-apollo'
